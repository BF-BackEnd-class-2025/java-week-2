/**
 * Exercises for practicing arrays in Java.
 *
 * Try to implement each exercise inside the main method.
 * Use System.out.println to print results and check your solutions.
 */

import java.util.Arrays;

public class ArraysE
{
    public static void main(String[] args)
    {
        // 1. Create an array of the first 10 natural numbers (1 to 10).
        //    Print the array using Arrays.toString().

        // 2. Find and print the largest number in the array: {5, 12, 7, 3, 25, 9}.

        // 3. Calculate and print the sum of all elements in an array: {2, 4, 6, 8, 10}.

        // 4. Reverse an array {1, 2, 3, 4, 5} and print the result.
        //    (Hint: swap elements from start and end moving towards the center)

        // 5. Search for a number in an array.
        //    Example: in {3, 8, 15, 23, 42}, search for 15 (expected: found).
        //             search for 7 (expected: not found).

        // 6. Copy an array and modify the copy without changing the original.
        //    Example: copy {1, 2, 3, 4}, then change the second element in the copy to 99.

        // 7. BONUS: Create a 2D array (matrix) 3x3 filled with numbers 1–9.
        //    Print it in a grid form, like:
        //    1 2 3
        //    4 5 6
        //    7 8 9
    }
}
